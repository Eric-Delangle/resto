<form method="post" action="{{ path('user_admin_delete', {'id': user.id}) }}" onsubmit="return confirm('Etes vous sur(e) de vouloir supprimer ce client ?');">
	<input type="hidden" name="_method" value="DELETE">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ user.id) }}">
	<button class="btn btn-danger">Supprimer ce client</button>
</form>
